services:
  - type: web
    name: ocean-quiz
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: ocean-quiz-db
          property: connectionString
      - key: NODE_ENV
        value: production
